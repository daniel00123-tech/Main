{
  "name": "@infra/caddington-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "download-base": "node scripts/download-base.mjs",
    "build": "node scripts/build-worker.mjs",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@infra/xero-core": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "wrangler": "^3.109.2"
  }
}
