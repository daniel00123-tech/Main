# Copy to .env.production for local production builds (gitignored).
# Production Pages uses same-origin /api proxy to https://api.infrastack.app.
# Leave empty so session cookies stay first-party on app.infrastack.app.
VITE_API_BASE=
